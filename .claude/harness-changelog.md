# 하네스 변경 이력

`CLAUDE.md` 의 하네스(pr-cycle·edge-review·docs-sync·codex-review-loop) 구성이 언제·왜 바뀌었는지 기록한다. 규약 본문은 [CLAUDE.md](../CLAUDE.md), Git 컨벤션 SSOT 는 [README.md](../README.md) 다.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-02 | 초기 구성 — pr-cycle·docs-sync 스킬 (경량 시작, 에이전트 팀 없음) | skills/pr-cycle, skills/docs-sync | git 히스토리 반복 작업 1·2순위 스킬화 |
| 2026-07-02 | 티켓 확인을 3단계 에스컬레이션으로 확장 (스프린트→백로그→생성, 스프린트 배치 보장) | skills/pr-cycle | 백로그 미확인 시 중복 이슈 생성 우려 피드백 |
| 2026-07-04 | edge 특화 코드리뷰 스킬 신설 + PR 전 게이트에 편입 (edge-review → docs-sync 2단계; 빌드/테스트 확인은 edge-review 안에 포함) | skills/edge-review, skills/pr-cycle | 내장 /code-review 대신 AGENTS 규칙·계약(schema SSOT·신뢰경계·레이크) 특화 리뷰를 게이트에 상시화 |
| 2026-07-10 | 파인더 각도 H(검증·품질 게이트 완전성) 신설 — malformed 입력의 crash-before-gate·coerce-to-passing·unchecked-field 우회를 적대적 열거 | skills/edge-review | ALPHA-133 정제 PR에서 edge-review가 놓친 게이트 우회 6건(비달력일·NaN/inf·소수거래량·비객체행·정체성결측 등)을 Codex가 전부 잡음 — 검증 게이트 특화 각도 부재가 원인 |
| 2026-07-15 | F각도(신뢰경계) 전면 개정 — widget-api 읽기전용·gateway 3라우트 전제를 하이브리드 경계(Sync 채널 outbound-Pull·인증서-테넌트 인가·Serving Published-only·데이터 거주지)로 교체, 대체된 ADR-0006 인용 제거, widget 예시를 tenant-console-api로 교체 | skills/edge-review, skills/pr-cycle, skills/docs-sync | 코드베이스 재편(widget 삭제, ADR-0010·0016)으로 구 신뢰경계 전제가 소멸 — 하네스가 폐기된 구조를 규칙 근거로 인용 중이었음 |
| 2026-07-17 | PR 생성과 머지 사이에 Codex 리뷰 왕복 단계 신설 — `+1` 리액션/리뷰 풀링, 수용 판단(과잉 엄밀 보정·의도적 생략 고려), 수용 시 수정→`@codex review` 재요청 반복, `+1` 또는 전건 비수용 시 머지 진행 (README 사이클도 8단계로 동기화) | skills/pr-cycle, README.md | PR마다 Codex 리뷰 대응을 수동으로 반복해 온 왕복을 사이클에 편입 |
| 2026-07-21 | Phase 6(Codex 왕복)을 `codex-review-loop` 스킬로 추출 — pr-cycle은 위임(통과 여부+비수용 목록 반환받아 머지 판단). 브랜치·커밋·머지 등 트리비얼 단계는 pr-cycle과 바뀌는 이유가 같아 추출하지 않음 | skills/codex-review-loop, skills/pr-cycle | SRP — Codex 봇 프로토콜(리액션·`@codex`·풀링)은 pr-cycle과 바뀌는 이유가 독립적. 유일한 정당한 경계라 추출·재사용, pr-cycle 본문 경량화 |
| 2026-07-21 | 이슈 생성 시 담당자·story point 필수화 — 담당자 불확실 시 사용자에게 확인, story point는 백로그·완료 이슈 2~3건을 기준선으로 비교 산정(근거 보고) | skills/pr-cycle | 담당자·SP 누락 이슈가 보드 소유권·스프린트 용량 계산을 깨뜨린다는 피드백 |
| 2026-07-22 | PR 전 게이트를 로컬 수렴 루프로 개편 — edge-review(게이트 기본 `high`·위험 변경 `max`, 트리비얼은 예외)를 수용 finding 0건까지 전체 범위 재리뷰로 반복(수정 라운드 상한 3회+검증 라운드 1회, 미수렴 시 사용자 확인), docs-sync는 루프 종료 후 1회, Phase 6 봇 왕복 기대치 1라운드 + 수렴 PR 에서 초과 시 각도 보강 제안 신설 | skills/pr-cycle, README.md | 실측(최근 10개 PR): 봇 finding 대부분이 로컬에서 잡히는 성격(#191 5라운드·8건, E·G·C각도 해당)인데 게이트가 medium 1회 실행이라 커버리지 부족 + 수정분 무재리뷰가 왕복 장기화(작업당 최대 ~1시간)의 주범 — GitHub 왕복을 대기 없는 로컬 라운드로 흡수 |
| 2026-07-23 | Phase 0 에 체크아웃 점유 감지 신설 — 무관 브랜치·미커밋 변경 발견 시 같은 체크아웃에서 전환 금지, Phase 2 worktree 분기 경로·8단계 worktree 정리 추가 (README "병렬 작업"이 규약 SSOT, README 사이클 3단계에 교차 참조 동기화) | skills/pr-cycle, README.md | ALPHA-515 세션이 pr-cycle Phase 2 를 그대로 따르다 ALPHA-513 진행 중이던 메인 체크아웃을 점유(16:16, 마지막 push 3분 뒤 — 운으로 무손상). README 병렬 규약이 있어도 스킬이 병렬 감지 없이 공유 체크아웃 전환을 지시하는 구멍 실증 |
| 2026-07-24 | C각도(교차 파일)에 계약-소비자 형상 대조 보강 — 계약·와이어 형상 변경 시 그 형상을 파싱하는 소비자 코드를 grep 해 필드명·중첩·타입·nullable 일치 확인 | skills/edge-review | ALPHA-395 에서 계약(nested document)이 소비자 publication-api ExplanationStore 파싱(flat kind/title/source/published_at)과 어긋난 걸 로컬은 DDL 정확성만 봐 놓치고 봇이 4-5라운드에 걸쳐 잡음 — 계약 리뷰에 소비자 파싱 대조 각도 부재가 원인 |
| 2026-07-25 | I각도(IaC 교체·상태 안전성) 신설 — terraform 변경이 ForceNew 속성(ami·subnet_id 등) 드리프트로 stateful 리소스를 destroy+recreate 하는 함정·most_recent/와일드카드 오선택·SecureString state 노출을 적대적 확인, Phase 0 트리거에 IaC 편입 | skills/edge-review | ALPHA-550 에서 AMI 필터 교정이 `ami`(ForceNew) 드리프트를 만들어 다음 apply 가 기존 박스+루트 EBS(PG 데이터)를 교체하는 P1 을 로컬 edge-review 가 놓치고 봇이 잡음 — IaC 특유의 교체·상태 손실 각도 부재가 원인 |
| 2026-07-25 | C각도(계약-소비자 형상 대조)에 기계 어서션↔서버 accept 집합 대조 보강 — 명세(openapi/JSON Schema) 작성·변경 시 `format`·`pattern`·`enum`·`minLength`·`required`·nullable 이 서버 검증 코드의 실제 accept/reject 집합과 일치하는지 대조(산문 완화는 기계 계약을 못 바꿈) | skills/edge-review | ALPHA-498 에서 openapi `format: date` 가 빈 `trade_date=` 를 무효로 모델링했으나 서버 `parseTradeDate` 는 `isBlank` 로 생략 처리(200) — 로컬이 형상만 보고 어서션-서버 불일치를 놓쳐 봇이 2라운드 왕복. 필드명·타입만 보던 C각도가 어서션 수준을 안 본 게 원인 |
| 2026-07-25 | J각도(프론트 서빙·빌드↔런타임 정합) 신설 — 프론트 서빙·패키징 변경 시 서빙 프로덕션 빌드의 인증경로 실재(dev 전용 헬퍼 스트립→죽은 껍데기)·빌드타임 baked 값↔런타임 서비스 설정 정합·서빙 번들 시크릿 노출 등급을 적대적 확인, Phase 0 트리거에 프론트 서빙·패키징 편입 | skills/edge-review | ALPHA-554 에서 서빙된 콘솔 SPA 가 prod 빌드에 로그인 UI 가 없어(dev 자동세션 스트립) 전 API 401 인 P1, compose 부트스트랩 비번 override 가 UI baked 비번과 갈리는 P2 를 로컬이 놓치고 봇이 3라운드에 걸쳐 잡음 — 서빙 빌드 동작·빌드↔런타임 정합 각도 부재가 원인 |
| 2026-07-27 | 변경 이력 표를 `.claude/harness-changelog.md` 로 분리 — CLAUDE.md 에는 링크 한 줄만 남김 | CLAUDE.md, .claude/harness-changelog.md | 표가 CLAUDE.md 의 88%(4.1k/4.7k자)를 차지하는데 세션 동작을 바꾸지 않는 이력 — 매 세션 항상 로딩되는 비용만 지불(약 1k 토큰). 사유 컬럼은 보존 가치가 있어 삭제가 아니라 이동. docs/ 가 아니라 .claude/ 인 이유는 docs/README.md 헌장(설계 지식 SSOT, 운영 규칙은 루트로)에 툴링 이력이 해당하지 않기 때문 |
| 2026-07-29 | D각도(Rule 9)에 단언 강도 대조 보강 — 테스트가 지키려는 계약의 반례를 현재 단언이 실제로 거부하는지 확인(`doesNotExist()` null 통과·존재 단언만으로 여분 키 방치·부분 매칭류) | skills/edge-review | ALPHA-599(M2)에서 "result 필드 부재" 계약을 `jsonPath().doesNotExist()` 로 단언 — null 명시도 통과하는 약한 단언을 로컬 게이트(수렴 3라운드)가 못 잡고 봇이 잡음. 단언 강도를 계약 강도와 대조하는 각도 부재가 원인 |
| 2026-07-29 | 7단계에 마이그레이션 단조성 재확인 신설 — 브랜치에 신규 마이그레이션이 있으면 머지 직전 최신 origin/dev 세트 최고 버전과 대조, 역행이면 전방 리네임 후 게이트 재진입 (README "스키마 마이그레이션 머지 게이트" 신설과 동기화) | skills/pr-cycle, README.md | schema-validate 의 단조성 guard 는 체크 시점 base 기준인데 branch protection(up-to-date 강제) 부재로 병렬 PR 머지 경합의 역행 착지 창이 열려 있음 — 2026-07-29 하루 #389·#391 로 3건 착지, dev schema-migrate CD 연속 실패(ALPHA-623). guard 주석이 예언한 한계가 실증됨 |
| 2026-08-01 | K각도(문서화된 실행 경로의 실행 가능성) 신설 + docs-sync 5단계에 같은 확인 편입 — diff 가 문서에 실행 명령·env 예시를 추가·변경하면 "그 명령을 그대로 쳤을 때 되는가"를 확인(셸 파싱·로더가 받는 형태·필수 전제), 확인 비용이 한 번이면 실제 실행. Phase 0 트리거에 편입 | skills/edge-review, skills/docs-sync | ALPHA-670 에서 새 relay 엔트리포인트의 README 예시가 `…__QUEUE_URLS__price-analysis-realtime=`(하이픈) 형태라 셸이 변수 할당으로 못 읽어 **첫 문서 실행 경로가 통째로 불가**였는데, 로컬 게이트 7라운드(수용 46건)가 코드만 보느라 놓치고 봇이 잡음. edge-review 는 코드를, docs-sync 는 문서-코드 일치를 보지만 '문서대로 하면 되는가'는 어느 각도도 안 봤음. 게이트 순서상(edge-review 루프 → docs-sync 1회) **docs-sync 가 나중에 쓰는 실행 예시는 K각도가 못 본다**는 사각을 봇이 지적해 docs-sync 5단계에도 같은 확인을 넣음. 이어서 "확인 비용이 한 번이면 실행" 문구가 한 줄짜리 `terraform apply` runbook 까지 돌리게 만든다는 지적을 받아 **부작용 없는 검증**(셸 파싱·설정 로드·--help/--dry-run/plan)으로 좁히고 상태 변경 명령은 사용자 승인 전 실행 금지로 명시. 이어 `bash -n` 이 `A__b-c=v true` 를 exit 0 으로 통과시킨다는 지적(실측 확인)을 받아 허용 방법 열거가 계속 구멍을 만들어(문법 검사가 하이픈 env·없는 모듈을 둘 다 exit 0 으로 통과) **목표 우선 서술로 재작성**: 문서에 쓴 실행 줄은 '되는 걸 확인' 아니면 '확인 못 함 보고' 둘 중 하나, 문법 검사는 확인이 아님, 상태 변경 명령은 실행 금지 |
