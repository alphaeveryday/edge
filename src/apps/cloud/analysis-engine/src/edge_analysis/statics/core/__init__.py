"""공유 인과귀속 엔진 — 창 설명(`window/`)과 독립 운영 스크립트(`ops/`)가 함께 쓴다.

closed vocab(`vocab`)·결정론 창(`windows`)·합=1 분해(`tree`)·3값 게이트(`gates`)부터
S3·RDB 결합(`duck`)·패널 검정(`paneltest`)·가설 제안(`hypothesize`)·서술 계약
(`narrate`·`plain`)까지, 인과귀속 파이프라인의 몸통이 여기 있다.
"""
