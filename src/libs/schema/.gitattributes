# ERD 생성물·스크립트는 OS 무관 LF 로 고정한다 — 드리프트 체크(schema-validate)가
# CRLF/LF 차이로 오탐하지 않도록. gen-erd.sql/generate-erd.sh 는 CI(Linux) 에서 실행되므로 LF 필수.
generated/*.dbml text eol=lf
scripts/*.sh text eol=lf
scripts/*.sql text eol=lf
