rootProject.name = 'edge'

// JVM 워크스페이스 루트 (ADR-0001 — 런타임별 루트 분리).
// 공용 라이브러리와 앱을 단일 멀티프로젝트 빌드로 묶는다.
// 런타임 배포는 여전히 서비스별 독립(각 앱이 자기 bootJar·이미지·ECS 서비스).
include ':libs:schema'
include ':libs:jvm-common'
// 앱은 배포 플레인(cloud/onprem)으로 그룹핑한다 (ADR-0029). 프로젝트 경로가 곧 디렉터리다.
include ':apps:onprem:tenant-console-api'
include ':apps:cloud:tenant-sync-api'
include ':apps:onprem:publication-api'
include ':apps:cloud:super-admin-api'
include ':apps:onprem:sync-agent'
include ':apps:onprem:intake'
